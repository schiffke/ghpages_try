name: Commit Date Committer

on:
  push:
    branches: [ ghpages_try ]
 
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2
      - name: run json generator
        run: github_api_handles.js
